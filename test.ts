/// <reference path="angular.d.ts"/>
module $ { 1:app.module } {
    'use strict';
    export default class AppController {
        static $inject = ['$scope'];
        constructor($scope: ng.IScope) {

        }
        action() {
        }
    }
}